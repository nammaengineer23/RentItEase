name: Release

on:
  # Temporarily manual-only while RC1 is the sole automatic workflow.
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create release
        uses: softprops/action-gh-release@v2
